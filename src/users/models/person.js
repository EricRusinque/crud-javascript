export class Person {

    /**
     * 
     * @param {Like<Person>} personDataLike 
     */

    constructor({author, quote}) {

        this.author = author;
        this.quote = quote;
    
    }

}